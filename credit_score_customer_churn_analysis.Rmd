---
title: "Customer Churn Analysis in the Banking Industry"
author: "Team: SNM Analytics"
# date: "today"
date: "`r Sys.Date()`"
output:
  html_document:
    code_folding: hide
    number_sections: false
    toc: yes
    toc_depth: 3
    toc_float: yes
  pdf_document:
    toc: yes
    toc_depth: '3'
---

```{r init, include=F}
library(ezids)
knitr::opts_chunk$set(warning = F, results = "hide", message = F)
options(scientific = T, digits = 3)
```

```{r}
dataset <- data.frame(read.csv("Data.csv"))
```

```{r}
str(dataset)
```

```{r}
rows <- nrow(dataset)
cols <- ncol(datset)
```

Our data set has `{r rows}` observation and `{r cols}` variables


```{r}
ezids::xkablesummary(dataset)
```



```{r}
with_balance_dataset <- dataset[dataset$Balance > 0, ]
str(with_balance_dataset)
```